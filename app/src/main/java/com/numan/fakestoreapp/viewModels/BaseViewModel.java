package com.numan.fakestoreapp.viewModels;

import androidx.lifecycle.ViewModel;

public class BaseViewModel extends ViewModel {

    //Intended for better error handling, having a global mutable state for error for each VM to show custom alerts for errors.
    //but this API doesn't throw meaningful error messages.

}
